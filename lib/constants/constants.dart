import 'package:flutter/material.dart';

const primaryColor = Color(0xFFFFC107);
const secondaryColor = Color(0xFF242430);
const darkColor = Color(0xFF191923);
const bodyTextColor = Color(0xFF8B8B8D);
const secondaryTextColor = Color(0xFF4F4F55);
const bgColor = Color(0xFF1E1E28);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0; // max width of our web

// Skills
const pythonPercent = 0.70;
const csharpPercent = 0.70;
const dartPercent = 0.25;
const blenderPercent = 0.6;
const arduinoPercent = 0.3456789;
const unityPercent = 0.4;
